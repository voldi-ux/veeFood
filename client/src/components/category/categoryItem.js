import React from 'react'
import './category.scss'

//the category model should look like this categoryName(i.e burgers): {items:[{burgers}]}

const CategoryItem = ({title,link,items,se}) => {

 return 
}

export default CategoryItem;