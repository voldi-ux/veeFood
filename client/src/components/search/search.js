import React from 'react'
import './search.scss'

//it should have a title that says, you searched for ${keyword} and we found 30 match results. 
//the search slice's initial state should look like this {searchData: [], keyword: keyword}
const SearchComponent = () => {
    return <h1>
        search component
    </h1>
}

export default SearchComponent;